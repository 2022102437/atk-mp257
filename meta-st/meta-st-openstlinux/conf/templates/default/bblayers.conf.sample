# LAYER_CONF_VERSION is increased each time build/conf/bblayers.conf
# changes incompatibly
LCONF_VERSION = "7"

BBPATH = "${TOPDIR}"
BBFILES ?= ""
OEROOT := "${@os.path.abspath(os.path.dirname(d.getVar('FILE')) + '/../..')}"

#--------------------------
# Layers var definition for buildsystem
#
OPENEMBEDDED = "${OPENEMBEDDED_NON_REMOVABLE}"

OPENEMBEDDED_NON_REMOVABLE = "${OEROOT}/atk-mp257/openembedded-core/meta"

POKY  = "${POKY_NON_REMOVABLE}"
POKY += "${OEROOT}/atk-mp257/poky/meta-yocto-bsp"

POKY_NON_REMOVABLE  = "${OEROOT}/atk-mp257/poky/meta"

#--------------------------
#
BASELAYERS ?= " \
    ${OEROOT}/atk-mp257/meta-openembedded/meta-gnome \
    ${OEROOT}/atk-mp257/meta-openembedded/meta-multimedia \
    ${OEROOT}/atk-mp257/meta-openembedded/meta-networking \
    ${OEROOT}/atk-mp257/meta-openembedded/meta-webserver \
"

BBLAYERS_NON_REMOVABLE ?= " \
    ${@'${OPENEMBEDDED_NON_REMOVABLE}' if os.path.isfile('${OEROOT}/atk-mp257/openembedded-core/meta/conf/layer.conf') else '${POKY_NON_REMOVABLE}'} \
  "

BSPLAYER ?= " \
    ${@'${OEROOT}/atk-mp257/meta-st/meta-st-cannes2' if os.path.isfile('${OEROOT}/atk-mp257/meta-st/meta-st-cannes2/conf/layer.conf') else ''} \
    ${@'${OEROOT}/atk-mp257/meta-st/meta-st-stm32mp' if os.path.isfile('${OEROOT}/atk-mp257/meta-st/meta-st-stm32mp/conf/layer.conf') else ''} \
    "

ADDONSLAYERS = ""

# specific to framework
FRAMEWORKLAYERS += "${@'${OEROOT}/atk-mp257/meta-st/meta-st-openstlinux' if os.path.isfile('${OEROOT}/atk-mp257/meta-st/meta-st-openstlinux/conf/layer.conf') else ''}"

# add BSP layer
BBLAYERS += " \
    ${BASELAYERS} \
    ${BSPLAYER} \
    ${ADDONSLAYERS} \
    ${FRAMEWORKLAYERS} \
    ${@'${OPENEMBEDDED}' if os.path.isfile('${OEROOT}/atk-mp257/openembedded-core/meta/conf/layer.conf') else '${POKY}'} \
    "
